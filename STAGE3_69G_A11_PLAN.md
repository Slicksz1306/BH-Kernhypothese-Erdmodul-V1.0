# Stage 3.69G / A11 – Time-Dependent WDM Partial-Sink PDE + EOS/Ionization Closure

**Status:** NEXT BLOCK / DEFINED / NOT YET CALCULATED

## Ziel

A11 soll die verbleibende A10-Luecke zwischen publiziert kalibriertem aeusseren Fe-Transport und der extrem komprimierten inneren Zone dynamisch schliessen.

```text
rho,T outer state
 -> EOS / Zbar / mu_e table
 -> diffusion / viscosity / conductivity / stopping
 -> time-dependent spherical mass + momentum + energy equations
 -> partial absorptive inner sink from A4/A5
 -> reflection/rethermalization generated by transport
 -> charge/composition advection
 -> dynamic backpressure
 -> species-resolved net Mdot_BH(t).
```

## Mindestgleichungen

```text
partial_t rho + 1/r^2 partial_r(r^2 rho u) = -S_capture

partial_t(rho u)
 + 1/r^2 partial_r[r^2(rho u^2 + P)]
 = -rho GM/r^2 + transport/source terms

partial_t E
 + 1/r^2 partial_r[r^2 u(E+P)]
 = Q_cond + Q_ei + Q_reaction - Q_capture
```

plus charge/composition advection fuer mindestens Fe/Ni/e-/p Reduced Species.

## Pflichtverbesserungen gegen A10

1. `Zbar(rho,T)` darf nicht als konstanter Wert eingesetzt werden.
2. Die tiefere EOS muss explizit als Tabelle/Interpolation oder klar markierte Extrapolation vorliegen.
3. `Kn=1` darf keine frei gesetzte reflektierende Grenze sein.
4. Backpressure muss aus der zeitabhaengigen Loesung entstehen.
5. Der innere Sink muss aus A4/A5 Capture-Wahrscheinlichkeiten gespeist werden.
6. Massenerhaltung und Energiebilanz muessen numerisch kontrolliert werden.

## Pflicht-Massenscan

```text
1e10 kg
1e11 kg
2e11 kg
5e11 kg.
```

Besonderes Augenmerk:

```text
1e10 kg:
    A9/A10 backpressure-sensitive

>=1e11 kg:
    A9/A10 reduced supply-processing branch
```

A11 soll testen, ob diese Trennung auch dynamisch bestehen bleibt.

## Acceptance Criteria

- stationaerer absorbing-Bondi-Grenzfall reproduziert;
- reflektierender Grenzfall reproduziert A7 Backpressure/Shock;
- partielle Sinkfaelle liegen numerisch zwischen den Grenzfaellen;
- globale Massenerhaltung innerhalb dokumentierter Toleranz;
- Energiefehler separat ausgewiesen;
- `Mdot_BH(t)` konvergiert gegen Gitter/Zeitschritt;
- EOS-/Zbar-Extrapolationsunsicherheit wird in ein Mdot-Band propagiert;
- kein Solver-PASS wird als experimentelle Bestaetigung bezeichnet.

## Ausgangsbefund aus A10

A10 findet selbst im schnellsten QMD-kalibrierten Transportenvelope:

```text
M=1e10 kg: Xi_high ~1.47
M=1e11 kg: Xi_high ~2.81e-3
M=2e11 kg: Xi_high ~4.28e-4
M=5e11 kg: Xi_high ~3.54e-5.
```

Die QMD-kalibrierte aeussere Zone ist bereits kollisionsoptisch dick. Daher soll A11 vor allem klaeren, ob **dynamischer Backpressure**, nicht freier ballistischer Escape, die Nettoakkretion reduziert.
